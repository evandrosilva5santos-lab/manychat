Liga/desliga uma etapa opcional do fluxo simples (lembrete, pedir e-mail, exigir seguir o perfil).

- Use dentro de `fx-setting`: a frase descreve o que o contato vai receber, o toggle fica à direita.
- `role="switch"` + `aria-checked`. Efeito imediato — nunca exija "Salvar" depois de um toggle.
