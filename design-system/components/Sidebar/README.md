Barra lateral de ícones do app (72px).

- Itens na ordem: Início, Contatos, Automações, Conversas (inbox), Transmissões, Configurações. Conta/avatar e ajuda ficam no rodapé.
- Ícones de traço 1.75px, 22px (padrão Lucide). Todo item tem `aria-label` e tooltip; o ativo usa `aria-current="page"`.
